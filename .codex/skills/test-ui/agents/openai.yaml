interface:
  display_name: "Test UI"
  short_description: "Run command-line UI tests and stop on the first output mismatch"
  default_prompt: "Use $test-ui to run the commands in test/ui-test-plan.md, compare each output with its expected output, and show the complete console session."
